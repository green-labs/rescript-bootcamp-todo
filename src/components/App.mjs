// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Todo from "./Todo.mjs";
import * as React from "react";
import * as Belt_Set from "rescript/lib/es6/belt_Set.js";
import * as TodoInput from "./TodoInput.mjs";
import * as Belt_Array from "rescript/lib/es6/belt_Array.js";
import * as TodoListItem from "./TodoListItem.mjs";

function App(Props) {
  var match = React.useState(function () {
        return Todo.todoSet;
      });
  var setTodos = match[1];
  var handleAddTodo = function (todoText) {
    setTodos(function (prev) {
          return Todo.addTodo(prev, todoText);
        });
  };
  var handleRemoveTodo = function (todo) {
    setTodos(function (prev) {
          return Todo.removeTodo(prev, todo);
        });
  };
  var handleToggleStatus = function (todo) {
    setTodos(function (prev) {
          return Todo.toggleStatus(prev, todo);
        });
  };
  return React.createElement("div", {
              className: "container"
            }, React.createElement("h1", {
                  className: "title"
                }, "RESCRIPT TO DO"), React.createElement(TodoInput.make, {
                  addTodo: handleAddTodo
                }), React.createElement("ol", {
                  className: "todosContainer"
                }, Belt_Array.map(Belt_Set.toArray(match[0]), (function (todo) {
                        return React.createElement(TodoListItem.make, {
                                    todo: todo,
                                    removeTodo: handleRemoveTodo,
                                    toggleStatus: handleToggleStatus,
                                    key: String(todo.id)
                                  });
                      }))));
}

var make = App;

export {
  make ,
}
/* Todo Not a pure module */
